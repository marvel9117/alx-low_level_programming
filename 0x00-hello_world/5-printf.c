#include <stdio.h>
/**
 * main - A program that prints out using printf
 *
 * Return - when return is equal to zero it means code is successful
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
